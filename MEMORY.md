# MEMORY

## Snapshot

- Repo root: `C:\Users\jinghongjie\Desktop\space-build`
- Unity project: `UnityProject/`
- Unity version: `6000.4.0f1`
- Render pipeline: `URP`
- URP package: `17.4.0`
- Main scene: `UnityProject/Assets/Scenes/BootstrapWorld.unity`

## MVP contract

- Project positioning: `showcase + minimal building gameplay MVP`
- Real environment assets are required from `models/scene`:
  - `stone01.fbx`
  - `tree01.glb`
- Player source asset:
  - `20260411144703_885c4d6d.fbx`
- Current runtime baseline:
  - `TopDownFollowCamera`
  - `SimpleCharacterMover`
- `FreeFlyCamera` is non-baseline only: keep it documented, if needed, as a debug or inspection utility rather than the MVP camera contract.

## Current implementation state

- Scene showcase baseline exists.
- Player roaming exists.
- Bootstrap-generated world is the current inspection scene.
- Minimal building gameplay now exists through `MinimalBuildSystem`.
- Visible ground is now generated as a tiled surface grid instead of one oversized plane.
- Build placement now uses a hidden chunked build surface instead of per-tile colliders.
- Build catalog input supports number keys `1-9` (if corresponding catalog entries exist).
- `GameplayRoot` is generated by `SceneBootstrap`.
- `BuildSystemRoot` is generated under `GameplayRoot` and owns:
  - `BuildAnchorsRoot`
  - `BuildPreviewRoot`
  - `PlacedStructuresRoot`
- The build system is configured in the scene with real `stone01`, `tree01`, `powerstation`, `solar_panel`, and `water_equipment` asset references.
- Scene bootstrap now generates a flat terrain baseline (no procedural elevation).
- Initial scene generation no longer auto-spawns tree/stone clusters or resource nodes.

## Asset pipeline facts

- Source models live under `models/`.
- Bootstrap copies them into Unity source folders under `Assets/Art/.../Source`.
- `stone01.fbx` uses Unity FBX import.
- `tree01.glb` needs a working glTF import path in Unity.
- `Assets/Art/Environment/Trees/Generated/Bootstrap_Tree01.prefab` should preserve the source `tree01.glb` node rotation (`+90° X`).
- `SceneBootstrap.ResolveCatalogRootRotation("tree", ...)` should stay aligned with the source GLB node rotation instead of zeroing it out, otherwise the tree lies on its side during placement.
- Bootstrap import now routes additional model classes:
  - `models/player/anims` -> `Assets/Art/Characters/Player/Animations`
  - `models/scene/landmark` -> `Assets/Art/Environment/Landmarks/Source`
  - `models/scene/terrain` -> `Assets/Art/Environment/Terrain/Source`
  - `models/scene/props` -> `Assets/Art/Environment/Props/Source`
- Ground theme textures now route as:
  - `map/layers/dirt01.png` -> `Assets/Art/Environment/Terrain/Themes/dirt01.png`
  - `map/layers/grass01.png` -> `Assets/Art/Environment/Terrain/Themes/grass01.png`
  - `map/layers/rocky01.png` -> `Assets/Art/Environment/Terrain/Themes/rocky01.png`
  - `map/layers/sand01.png` -> `Assets/Art/Environment/Terrain/Themes/sand01.png`
- `models/building/*` -> `Assets/Art/Buildables/*/Source`
- `models/building/base-building/*` -> `Assets/Art/Buildables/Building/Source`
- Placeholder environment meshes do not satisfy the MVP contract.

## Authority / drift note

- Scene authority for the foundation pass is `SceneBootstrap` plus rerun-bootstrap output.
- The generated baseline is the source of truth when docs, serialized scene state, and older notes drift apart.
- Keep `README.md` and `MEMORY.md` aligned to that generated baseline instead of preserving stale free-fly wording.

## Control notes

- Top-down camera:
  - follows player center continuously
  - mouse wheel zoom
  - RMB yaw rotate
  - current bootstrap baseline tuned to a closer trapezoid-like view: `height 16 / distance 14 / pitch 60`
  - close zoom floor is now `minHeight 4 / minDistance 2.5`
- Character mover:
  - `WASD` now applies immediate constant-speed planar movement without acceleration ramp-up
  - movement facing is enabled in the current baseline and snaps to the nearest 8 directions (`45°` steps)
  - move-facing rotation is immediate instead of smoothed
  - `Space` jump
  - `Shift` sprint
  - player runtime now drives `MoveSpeed` animator parameter directly from live movement input state, so holding any movement key keeps the run state active until all movement keys are released
  - `MoveSpeed` animator damping is disabled in the current baseline so run/idle switching responds immediately with input
  - imported player clips `running.fbx` and `standing-by.fbx` are now reconfigured by bootstrap to use looping clips
- Minimal build loop:
  - `1-9` select build catalog entry
  - `1`: `stone01`
  - `2`: `tree01`
  - `3`: `powerstation`
  - `4`: `solar_panel`
  - `5`: `water_equipment`
  - `R` rotate next placement
  - `LMB` place selected model
  - `MMB` or `X` delete nearest placed model

## 2026-04-21 base-building integration note

- `BootstrapProject.GetDestinationFolderForSource()` now routes `models/building/base-building/*` into `Assets/Art/Buildables/Building/Source`.
- `ValidateBootstrapState()` now treats the base-building pack as a verified bootstrap contract:
  - imported files must exist at:
    - `Assets/Art/Buildables/Building/Source/powerstation.fbx`
    - `Assets/Art/Buildables/Building/Source/solar-panel.fbx`
    - `Assets/Art/Buildables/Building/Source/water-equipment.fbx`
  - each target FBX must load as a Unity `GameObject`
  - generated scene catalog must be exactly 5 ids:
    - `stone01`
    - `tree01`
    - `powerstation`
    - `solar_panel`
    - `water_equipment`
- Added editor-side smoke verification entry point:
  - `SpaceBuild.Editor.BootstrapDiagnostics.ValidateBaseBuildingCatalogPlacement`
- Verification evidence for this pass:
  - `unity-base-building-bootstrap.log`
  - `unity-base-building-bootstrap-rerun.log`
  - `unity-base-building-verify.log`

## 2026-04-19 foundation pass notes

- `BootstrapWorld.unity` now serializes:
  - camera `pitch: 60`
  - camera `minHeight: 4`
  - camera `minDistance: 2.5`
  - build catalog `tree01` rotation offset reset to `{0,0,0}`
- bootstrap now generates stable fallback assets for low-risk visual recovery:
  - `UnityProject/Assets/Materials/Generated/Bootstrap_StoneFallback.mat`
  - `UnityProject/Assets/Art/Environment/Rocks/Generated/Bootstrap_Stone01.prefab`
  - `UnityProject/Assets/Art/Environment/Rocks/Generated/Bootstrap_Stone02.prefab`
  - `UnityProject/Assets/Art/Environment/Trees/Generated/Bootstrap_Tree01.prefab`
- player and tree now use source-model materials/colors directly; scene bootstrap no longer forces their fallback color overrides.
- stone catalog now supports multiple generated variants and placement rerolls from the stone pool after each successful placement.

## 2026-04-19 ground tile refactor notes

- The old visible `GroundPlane` path was replaced by `GroundTilesRoot` plus per-tile build surfaces.
- Hidden `Terrain` remains only as a flat sampling baseline; its renderer and collider are disabled after bootstrap.
- Ground tile config asset now lives at:
  - `UnityProject/Assets/Settings/GroundTileConfig.asset`
- Current default ground tile config:
  - `theme: Rocky`
  - `tileWorldSize: 7`
  - `gridColumns: 36`
  - `gridRows: 36`
- Bootstrap now generates four terrain theme materials:
  - `Bootstrap_Ground_Dirt.mat`
  - `Bootstrap_Ground_Grass.mat`
  - `Bootstrap_Ground_Rocky.mat`
  - `Bootstrap_Ground_Sand.mat`
- Ground tiles are named `GroundTile_{x}_{z}` and are tagged/layered as `BuildSurface`.
- Generated tree prefab now has root scale `5,5,5` for demo readability.
- `MinimalBuildSystem` now serializes per-entry random placement scale range:
  - `stone01: 0.9 -> 1.1`
  - `tree01: 0.9 -> 1.1`
- Preview objects stay stable; random scale only applies to the final placed instance.

## 2026-04-19 performance pass notes

- Visible ground tiles no longer own build placement or physics authority.
- `SceneBootstrap` now creates:
  - `GroundTilesRoot` for visible tiled ground only
  - `BuildSurfaceRoot` for hidden build raycast chunks
- 原先进入游戏时可见的 `BuildZone` 深色矩形提示块已从 bootstrap 场景中移除，不再作为初始视觉元素生成。
- Hidden build surface currently uses `4 x 4` chunked `BoxCollider` coverage across the whole ground footprint.
- Ground tiles now:
  - remove per-tile colliders
  - use `Untagged` instead of `BuildSurface`
  - disable shadow casting and shadow receiving
  - mark generated tile hierarchy static
- `MinimalBuildSystem` runtime optimizations:
  - `TryGetBuildSurfaceHit()` now uses single `Physics.Raycast(...)`
  - preview bottom offset is cached when preview object rebuilds
  - preview no longer recalculates renderer bounds every frame for snap-to-surface
  - new config field: `showDebugHud` default `true`
- Tree 5x root scale and placed object `0.9 -> 1.1` random scale remain intact after the performance pass.

## 2026-04-19 placed object performance v2 notes

- 放置物不再建议继续走“`MinimalBuildSystem` 直接长期持有真实实例 + 直接 `Instantiate/Destroy`”路线。
- 新的放置物性能架构由以下部分组成：
  - `PlacedObjectManager`
  - `PlacedObjectPerformanceConfig.asset`
  - stone/tree 自动生成的近景源 prefab + 中远景代理 prefab
- `MinimalBuildSystem` 当前职责已收敛为：
  - 输入处理
  - 预览与贴地
  - 放置请求
  - 删除请求
- 实际放置后的生命周期现在交给 `PlacedObjectManager`：
  - 记录 `catalogId / position / rotation / scale`
  - 按二维 chunk 组织放置物
  - 依据距离带切换 `Near / Mid / Far / Dormant`
  - 通过对象池回收和复用实例
- 当前默认性能配置资产位置：
  - `UnityProject/Assets/Settings/PlacedObjectPerformanceConfig.asset`
- 当前默认参数：
  - `chunkSize: 28`
  - `visibilityUpdateInterval: 0.15`
  - `lod0Distance: 22`
  - `lod1Distance: 45`
  - `lod2Distance: 70`
  - `lodHysteresis: 5`
  - `maxTrackedChunkRadius: 4`
  - `maxPoolSizePerType: 128`
- `SceneBootstrap` 当前会生成并写入：
  - `PlacedStructuresRoot`
  - `PlacedStructuresPoolRoot`
  - `PlacedObjectManager`
  - `Bootstrap_Stone_LOD1.prefab`
  - `Bootstrap_Stone_LOD2.prefab`
  - `Bootstrap_Tree01_LOD1.prefab`
  - `Bootstrap_Tree01_LOD2.prefab`
- stone/tree catalog 现已序列化：
  - `performanceCategory`
  - `lod1Prefab`
  - `lod2Prefab`
- 当前运行时实现采用“manager 距离带切换代理 prefab + dormant 休眠”的方式，重点是降低活跃 renderer / collider / script 数量，并避免视角变化时的大量创建销毁抖动。
- 为了让 `SceneBootstrap` 能直接引用这些新运行时类型，当前脚本程序集已拆分为：
  - `UnityProject/Assets/Scripts/Runtime/SpaceBuild.Runtime.asmdef`
  - `UnityProject/Assets/Scripts/Editor/SpaceBuild.Editor.asmdef` -> 显式引用 `SpaceBuild.Runtime`

## 2026-04-19 foundation pass verification

- Verified batch bootstrap baseline is `Unity 6000.4.0f1`, not `6000.0.41f1`.
- Batch bootstrap on `6000.0.41f1` failed during package resolution for builtin module dependencies; treat that path as stale for this project.
- Clean rerun on `6000.4.0f1` completed with exit code `0` and final log lines:
  - `[SceneBootstrap] Scene generated: Assets/Scenes/BootstrapWorld.unity`
  - `[ProjectBootstrap] Complete. Imported/updated assets: 0. Scene: Assets/Scenes/BootstrapWorld.unity`
- Verified generated/runtime-bound assets exist after rerun:
  - `UnityProject/Assets/Art/Characters/Player/Generated/Bootstrap_Player.prefab`
  - `UnityProject/Assets/Art/Environment/Trees/Generated/Bootstrap_Tree01.prefab`
  - `UnityProject/Assets/Materials/Generated/Bootstrap_RockyGround.mat`
  - `UnityProject/Assets/Materials/Generated/Bootstrap_StylizedSky.mat`
  - `UnityProject/Assets/Shaders/Bootstrap_VertexColorUnlit.shader`
- Serialized scene confirms:
  - `StylizedDayNightCycle` is attached and references `Bootstrap_StylizedSky.mat`
  - `BuildSystemRoot` contains `MinimalBuildSystem`
  - build catalog order is `stone01` first, `tree01` second, matching key `1` / `2`
- Generated tree prefab currently applies a `-90` degree X-axis root correction in prefab overrides; this is engineering-verified but still worth one final visual pass in-editor.

## 2026-04-19 Batch 0 asset validation

- Batch 0 crate route validation is now grounded with a real Sketchfab sample:
  - source uid: `aa2a89548f2e4f26bf42d30d5c095e73`
  - source model: `Hero Crate`
- Final validated repo artifact:
  - `models/scene/props/static/crate01.glb`
- Route verdict:
  - `Route A (direct GLB probe)`: pass
  - `Route B (Sketchfab -> Blender -> GLB export)`: pass, rerun hash-stable
- Current routing decision for future model batches:
  - `default route`: `B-blender`
  - `fallback route`: `A-direct`
  - `Batch 1 gate`: `GO`
- Evidence files:
  - `models/scene/props/static/crate01.batch0.json`
  - `.omx/context/batch0-run1.json`
  - `.omx/context/batch0-run2.json`
  - `.omx/context/batch0-crate-validation-summary-20260419.md`
- Reusable Blender-side validation helper:
  - `scripts/blender_batch0_crate_validation.py`

## Known limitations

- `stone01.fbx` references missing external textures, so rock materials are not final quality.
- `tree01.glb` reads small at source scale and will need scale-up for scene readability.
- Ground is currently using generated smooth low-poly terrain textures; a stronger final look likely still needs externally sourced stylized terrain assets/materials.
- Build placement currently has no grid snapping, no save data, and no economy/resource rules.
- Ground tile visuals were batch-verified through scene rebuild and asset serialization, but final in-editor eye check is still preferred for exact proportion feel.
- No save system, harvesting, survival, networking, or production content pipeline yet.

## Validation baseline

- Open `BootstrapWorld.unity` and confirm real tree and stone assets are present.
- Confirm at least one landmark is present (landmark model if imported, otherwise fallback enlarged stone landmark).
- Enter Play Mode and verify `TopDownFollowCamera` plus `SimpleCharacterMover`.
- Confirm the player spawns near the center area.
- Confirm the stylized sky/day-night cycle is active.
- Reject any bootstrap result that only shows fallback primitives.
- Confirm `BuildSystemRoot` contains `MinimalBuildSystem`.
- Verify key `1` maps to stone and key `2` maps to tree.
- Verify both `stone01` and `tree01` can be placed and nearest placed pieces can be removed cleanly.

## Next likely steps

1. Continue filling `models/RESOURCE_CHECKLIST.md` with real assets and rerun full bootstrap after each batch.
2. Stabilize the real asset import path for `tree01.glb`.
3. Improve `stone01.fbx` material coverage by restoring or rebuilding textures.
4. Add grid snapping, placement bounds, and proper build-piece prefabs when moving beyond MVP.

## Animal animation export note

- `models/animals/cow/static/cow01.fbx` and `models/animals/sheep/static/sheep01.fbx` are rigged source meshes without authored motion clips.
- Generated reusable Unity clips now exist at:
  - `UnityProject/Assets/Art/Animals/Generated/cow/cow01_walk.anim`
  - `UnityProject/Assets/Art/Animals/Generated/sheep/sheep01_walk.anim`
- External animated FBX outputs were exported to outer-layer usable paths:
  - `models/animals/cow/cow01.fbx`
  - `models/animals/sheep/sheep01.fbx`
- Validation pass: Unity re-import recognized animation clips `cow01_walk` and `sheep01_walk` from those exported FBX files.
- Export used a temporary local Unity FBX Exporter package hookup and was cleaned up afterward; project manifest was restored after generation.

## URP upgrade verification note

- Local rollback point for current version-line acceptance:
  - `UnityProject-backup-before-urp-upgrade-20260419-093735/`
- `UnityProject/ProjectSettings/URPProjectSettings.asset` currently records:
  - `m_LastMaterialVersion: 10`
- `GraphicsSettings.asset` still binds URP asset guid `4b83569d67af61e458304325a23e5dfd`
- `GraphicsSettings.asset` still binds URP global settings guid `18dc0cd2c080841dea60987a38ce93fa`
- `QualitySettings.asset` Mobile / PC quality levels both still bind the same URP asset guid
- Clean batch open on Unity `6000.4.0f1` completed with exit code `0` and no repeated `URP Material upgrade` prompt in the log
- After clean open, observed on-disk additions include generated fallback materials/prefabs under:
  - `UnityProject/Assets/Materials/Generated/`
  - `UnityProject/Assets/Art/*/Generated/`
- Treat those generated assets as part of the new local baseline unless they are intentionally rolled back together with the full backup directory.

## 2026-04-19 demo foundation bugfix note

- Legacy note: `GroundPlane` authority has been superseded by `BuildSurfaceRoot` chunk colliders.
- Current placement contract:
  - scene serialized `placementMask.m_Bits = 64`
  - runtime `MinimalBuildSystem` requires collider tag `BuildSurface`, not layer-only hits
- Tree placement orientation fix is two-part:
  - generated tree prefab keeps the `-90` degree root correction
  - runtime placement now composes surface/yaw rotation with the selected template's authored local rotation instead of overwriting it
- Player white-model fix:
  - source FBX initially imported as a URP/Lit material with empty texture slots
  - bootstrap now calls `ModelImporter.ExtractTextures(...)` into `UnityProject/Assets/Materials/Generated/ExtractedTextures/20260411144703_885c4d6d/`
  - generated `Bootstrap_PlayerFallback.mat` binds extracted albedo + normal textures
  - generated `Bootstrap_Player.prefab` now overrides renderer material to `Bootstrap_PlayerFallback.mat`
- Tree material fix:
  - `Bootstrap_TreeFallback.mat` now binds embedded GLB textures and is referenced by `Bootstrap_Tree01.prefab`
- Non-interactive validation added in `Assets/Scripts/Editor/BootstrapDiagnostics.cs`:
  - `LogBootstrapAssetAudit` logs source/generated renderer material chains
  - `ValidatePlacementSurfaceContract` instantiates stones and verifies `TryGetBuildSurfaceHit` still resolves `GroundPlane` / `BuildSurface`

## 2026-04-20 Blender MCP low-poly pipeline note

- Local Blender MCP server is reachable in the current desktop session on `127.0.0.1:9876`.
- Desktop session is not running OMX runtime Ralph loop; treat `$ralph` here as a workflow hint, not a live OMX runtime mode.
- Current Blender MCP panel state:
  - `Sketchfab` enabled
  - `Hunyuan3D` disabled
  - `Hyper3D Rodin` disabled
  - `PolyHaven` disabled
- This means text/image-to-3D generation services are not currently available, but procedural low-poly generation through Blender Python is available.
- Verified export path for generated low-poly scene stone:
  - `models/scene/stone/rock_generated_01.glb`
- Verification evidence:
  - Blender object `rock_generated_01` exported successfully as GLB
  - mesh stats: `12` vertices / `20` polygons

## 2026-04-23 ground visual surface simplification note

- `SceneBootstrap` no longer generates `GroundTilesRoot` with `GroundTile_{x}_{z}` visual instances.
- Visible ground now uses:
  - `GroundVisualRoot`
  - `GroundVisualPlane`
- Ground footprint still comes from `GroundTileConfig`:
  - `tileWorldSize * gridColumns` => total width
  - `tileWorldSize * gridRows` => total depth
- Current default footprint remains `252 x 252` world units from `7 * 36`.
- Visible ground material is now isolated through:
  - `UnityProject/Assets/Materials/Generated/Bootstrap_Ground_Visual.mat`
- Theme source materials remain:
  - `Bootstrap_Ground_Dirt.mat`
  - `Bootstrap_Ground_Grass.mat`
  - `Bootstrap_Ground_Rocky.mat`
  - `Bootstrap_Ground_Sand.mat`
- `Bootstrap_Ground_Visual.mat` copies the selected theme material and applies texture tiling from `gridColumns/gridRows`, so shared theme assets are not repurposed as the scene-specific visual surface.
- Hidden `Terrain` still exists only as a flat sampling baseline.
- `BuildSurfaceRoot` and its `4 x 4` `BuildSurfaceChunk_*` colliders remain the only build placement authority.
- `MinimalBuildSystem` was intentionally not changed in this pass.
- Current default rocky ground visual now sources `map/layers/lunar-surface.png` through bootstrap import into `Assets/Art/Environment/Terrain/Themes/lunar-surface.png`.

## 2026-04-25 placed-object LOD removal note

- `PlacedObjectManager` no longer swaps placed objects between LOD/low-model prefabs by camera distance.
- Placed objects now register a single `sourcePrefab`; distance logic only controls active/dormant visibility and pooling.
- `MinimalBuildSystem` catalog entries no longer serialize `lod1Prefab` or `lod2Prefab`.
- `SceneBootstrap` no longer generates or binds stone/tree LOD proxy prefabs.
- Removed generated low-model prefabs:
  - `UnityProject/Assets/Art/Environment/Rocks/Generated/Bootstrap_Stone_LOD1.prefab`
  - `UnityProject/Assets/Art/Environment/Rocks/Generated/Bootstrap_Stone_LOD2.prefab`
  - `UnityProject/Assets/Art/Environment/Trees/Generated/Bootstrap_Tree01_LOD1.prefab`
  - `UnityProject/Assets/Art/Environment/Trees/Generated/Bootstrap_Tree01_LOD2.prefab`
